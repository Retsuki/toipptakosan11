overwrite: true
schema: "schema.graphql"
documents: "graphql/**/*.graphql"
generates:
  graphql/generateTypes.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
