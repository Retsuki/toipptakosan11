overwrite: true
schema: "schema.graphql"
documents: "graphql/**/*.graphql"
generates:
  graphql/client.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
